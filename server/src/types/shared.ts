// The protocol and game catalogs shared with the client live in shared/types.ts — the one copy.
// Re-exported here so server code imports from this file as usual.
export * from '../../../shared/types';
