// The protocol and game catalogs shared with the server live in shared/types.ts — the one copy.
// Re-exported here so client code imports from this file as usual.
export * from '../../../shared/types';
